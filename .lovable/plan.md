Remove the Load Optimizer feature from the application entirely.

1. Remove the sidebar nav item from `src/components/layout/AppSidebar.tsx`.
2. Remove the route registration from `src/App.tsx`.
3. Delete the page component file `src/pages/LoadOptimizer.tsx`.

No database changes are required.